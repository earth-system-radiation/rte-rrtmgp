sudo: required
dist: xenial
language: cpp
matrix:
  include:
  - compiler: gcc
    env:
    - FC=gfortran-8
    - FCFLAGS=-ffree-line-length-none -m64 -std=f2003 -march=native
    addons:
      source:
      - ubuntu-toolchain-r-test
      apt:
        packages:
        - gfortran-8
script:
  - cd build
  - make
