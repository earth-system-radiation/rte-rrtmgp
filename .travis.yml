language: cpp
matrix:
  include:
  - compiler: gcc
    env:
    - FC=gfortran-9
    - FCFLAGS=-ffree-line-length-none -m64 -std=f2003 -march=native
    addons:
      apt:
        packages:
        - gfortran-9
script:
  - cd build
  - make 
