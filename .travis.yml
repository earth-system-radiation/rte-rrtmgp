sudo: required
dist: xenial
language: cpp
matrix:
  include:
  - compiler: gcc
    env:
    - FC=gfortran
    - FCFLAGS=-ffree-line-length-none -m64 -std=f2003 -march=native
script:
- gcc --version
- cd build
- make
